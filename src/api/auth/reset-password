// const { email, newPassword } = req.body; // zaten var
const salt = await bcrypt.genSalt(10);
const hashedPassword = await bcrypt.hash(newPassword, salt);

user.password = hashedPassword;
user.resetCode = null;
user.resetExpires = null;
await user.save();
